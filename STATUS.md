# Fandex — Status

_Your index of every game, movie & show._ · High-level roadmap. **Full detail + completion history live in [TASKS.md](TASKS.md).**

**👉 2026-07-27: H1.6f done — H1.6 (the whole UI overhaul) is COMPLETE.** Cross-cutting QA pass (Opus, main loop) that found **three real bugs no test suite would have caught**, all browser-only: (1) **contrast failures that were unreadable, not just dim** — past-month dividers and weekday labels rendering at **1.27:1**, poster/cast placeholders at 1.48:1 (under even the 3:1 icon bar); the 404 page turned out never to have been retokened at all, since it sits outside the IA route list; (2) **a reduced-motion hole the global CSS rule cannot close** — an explicit `behavior:"smooth"` in a `scrollIntoView`/`scrollBy` call *overrides* the CSS property, so all 4 JS-driven smooth scrolls silently escaped it; (3) **SM8 — the Back button was trapped on every gated page**: auth gates used `router.push`, so an anon visitor who tapped a gated nav item could never get Back to where they came from (9 sites → `replace`; verified history delta 2 → 1 and Back now returns correctly). Plus, per your calls: **44px tap areas** via invisible hit-area padding (the "Clear search" button was a **16×16** target; measured zero overlapping regions at 375/500/1280 afterwards — two collision traps caught by measuring rather than assuming), and a new **"Recently added" sort now defaulting Library** (kept out of the shared sort set and off the server enum on purpose). Q3 fixed: Library no longer auto-scrolls to today (Wishlist still does — it's forward-looking by design). A 4th **smoke sweep** ran anon-only and logged SM8 + SM9. Full gate: typecheck, **0 lint errors, 314 tests**, and `next build` compiles. **Open: SM9** (`/` shows a different tab title on fresh load vs client nav — cosmetic, your call) and **ActionCells' 32/36px tap targets** (only measurable logged-in). **The whole logged-in surface still needs your session** — four consecutive sweeps have been anon-only because the cookie-mint is hard-blocked; `smoketest.md`'s section C now lists exactly what to check.

**Previously: 2026-07-26: H1.6e is fully done — all 10 pages/surfaces shipped in one Sonnet session**, including **A5** (Discover search now returns typed People/Tags/Titles groups, reusing the existing `/api/discover/facets` catalog-vocab search — signed-in only, anon gets today's Titles-only search unchanged). See [ui-overhaul.md §10](docs/ui-overhaul.md) for the full per-page changelog. Highlights:
- **Home** (net-new): three rails (Popular/Upcoming/Recommended-for-you) off a new `/api/home` route reusing `/api/discover`'s machinery, a signed-in stats strip + best-genre card, anon gets the same public rails below the sign-in hero.
- **Calendar**: type-chip filter + a real error/empty state (the fetch previously had no error handling at all).
- **Profile hub**: stats + a "Coming up" mini-list + a "Recommended for you" mini-rail, reusing Home's/Calendar's data.
- **Item detail, facet pages (person/tag/studio), Insights, Settings**: all were still on 100% pre-token (pre-H1.6) styling — retokened all of them. Found and fixed **real bugs along the way, not just cosmetics**: a hardcoded `bg-[#0a0a0a]` on item-detail + facet pages that visibly mismatched every other page's background; `text-neutral-500/600` used directly on body text, which fails AA (the exact contrast bug H1.6d already fixed once on `ListCard`); facet pages' crowd-average stat cards colored with literal default-Tailwind blue/green — a direct violation of H1.6b's own "crowd rating is metadata, uncolored" rule.
- **Library/Wishlist**: added the D-A tab switcher (Library ⇄ Wishlist) both pages' own comments had flagged as deferred — a navigation affordance, not a state merge; each page keeps its own route/filters/fetch.
- Every page: typecheck clean, lint 0 errors, 312 tests passing throughout. Public pages (Home, item detail, facet pages) got full browser verification including computed-style checks; logged-in-only pages (Calendar, Profile, Insights, Settings, Library, Wishlist) had their anon auth-gate redirect verified live — the populated authed views still need Nils's own session, same standing constraint as every other logged-in surface in this repo.
- **Left open, by design:** `TagAdminControls`'/Settings' bespoke modals not migrated onto the `Sheet` primitive (flagged, not silently dropped). Settings' `logout` dead-code and a game-type item-detail page weren't touched/spot-checked (pre-existing, out of this pass's scope). **Next up is H1.6f** — cross-cutting QA (a11y sweep, Q3's default-sort/scroll fix, delete the orphaned `SearchModal.tsx`, full build, a `/smoketest` sweep) — **Opus/main-loop per AGENTS.md**, not Sonnet.

**Previously: 2026-07-24: H1.6c's nav + IA restructure shipped (Opus, main loop).** The whole app now runs on ONE adaptive `AppNav` — a desktop top bar / mobile fixed bottom bar (Home·Search·Calendar·Library·You), moved into the root layout as a global shell with a skip-to-content link, replacing the old per-page top NavBar (now deleted; item-detail pages gained a nav they never had). The IA moved with it: `/dashboard`→308→`/wishlist` (content moved), new `/calendar` and `/profile` hub routes, and `/` is now the public browse Home (was a login-only landing). Post-login still lands on `/wishlist` (keeps the onboarding flow); Home/Calendar/Profile are minimal placeholders for now — their rich content is H1.6e as planned. Scope confirmed with you before starting. Browser-verified anon at desktop + 375px mobile (both nav layouts matching the mockup, `aria-current`, the 308 redirect, anon "You"→sign-in dialog); typecheck + 307 tests + lint all clean. **Then A2's "Already-rated" filter shipped too — H1.6c is now fully done.** Added a third tri-state (Any/Only/Hide) "Rated" dimension to the discovery membership filter, threaded through all six membership sites (schema, server catalog + facet-external filters, the two client-side list/feed filters, and the FilterPanel UI), with 5 new regression tests. Browser-verified anon on Discover: "Rated: Only" correctly empties the feed (anon has no ratings), "Any" restores it. 312 tests + typecheck + lint all clean. **Next up is H1.6e** — page-by-page build-out with all four states (default/loading/empty/error), including fleshing out the minimal Home/Calendar/Profile placeholders this pass created and applying decision D-E's card score-slot fix. Much of H1.6e is Sonnet-delegable per the plan.

**Previously: 2026-07-24: two more H1.6d items shipped (Sonnet) — CalendarView's Agenda view + skip-to-previous-month, and FilterPanel's mobile bottom-sheet conversion.** Calendar: Month grid fully retokened (today = accent ring per spec); new grouped Agenda list view (This week/Next week/month buckets) with the design's Agenda `BellPlus` repurposed to add-to-wishlist per D-C; the board's flagged "no skip-to-previous-month" gap fixed with a symmetric pill. FilterPanel: a new `useMediaQuery` hook lets the SAME stateful advanced-filters content render in exactly one of two responsive locations (desktop inline / mobile inside the existing `Sheet` primitive) instead of ever risking two live copies — the "Filters" button on mobile now opens a real bottom sheet. Dead code cleanup: `ItemBadges.tsx` deleted, `Tooltip.tsx` retokened to match. Browser-verified anon at desktop + 375px mobile throughout (both calendar views, the toggle, anon wishlist-toggle failure path, the sheet opening/closing/resizing across the breakpoint), zero console/server errors, tests/typecheck/lint all clean. **H1.6d is now complete.** A2's tri-state Wishlisted/Already-rated filter turned out to be real backend work (the `membership` shape it'd extend threads through ~10 files including `discovery.ts`'s scoring engine) rather than Sonnet-safe UI wiring — Nils's call: **bundle it into H1.6c** instead of doing it here. **Next up is H1.6c** (adaptive nav + IA restructure + now A2) — **needs a full-effort Opus main-loop session, not Sonnet.** Ready to switch models whenever you want to pick that up.

**Previously: 2026-07-23: H1.6b shipped — the full primitive kit is built on the new tokens.** Button, Sheet (bottom-sheet mobile/modal desktop with focus trap), Menu, TagChip, TriToggle, Skeleton, Avatar, and a new ErrorState are all live; ConfirmDialog now runs through Sheet and picked up proper focus-trapping for free. Browser-verified end to end (search → empty state → clear button all working on `/discover`). Next: H1.6d (content components — cards, the new Rail, calendar Agenda view) — H1.6c (nav/IA restructure) is being held for a full-effort session per AGENTS.md, since it's auth-adjacent.

**Previously: 2026-07-23: H1.6a shipped — tokens, fonts and the icon library are live.** Design tokens are in `globals.css`, DM Serif Display/Space Grotesk/Space Mono are wired via `next/font`, lucide-react is confirmed ready. **Found and fixed a real bug on the way**: the design's named spacing scale collided with Tailwind v4's own reserved namespace and was silently collapsing `max-w-sm`/`md`/`lg`/`2xl`/`3xl` app-wide (24rem → 8px) — caught by opening the login page in a browser, not by the type system. Fixed, browser-verified, tests/typecheck/lint all clean. Detail in [docs/ui-overhaul.md](docs/ui-overhaul.md) §10. Next: H1.6b (primitive kit).

**Previously: 2026-07-23: the design handoff landed — H1.3/H1.4/H1.5 are all closed, H1.6 is planned.** Direction is **2a "Ticket · Calm"** (warm near-black, editorial serif, one brass-gold accent) and the bundle at [docs/design/fandex-handoff/](docs/design/fandex-handoff/) doubles as the locked design system: tokens, 15 components with every state, 10 mobile mockups, and AA acceptance criteria. I reconciled it against the live code — **4 of its 8 unverified data assumptions already hold, 4 don't** (no reminder system at all, no watch prices, no Insights "hours", and the design's fixed score band contradicts Q19's baseline-relative center), plus two more gaps it didn't flag. H1.6 is now 6 sub-tasks, ~190k, in [docs/ui-overhaul.md](docs/ui-overhaul.md) §10, and **all four gating decisions are answered** (§8): the design wins on the nav (wishlist becomes a Library tab), the score keeps its baseline-relative bands, reminders/prices are cut with the Agenda bell repurposed to add-to-wishlist, and desktop gets derived in code. **Ready to start at H1.6a.** Deploys stay paused until ~1 Aug, so this is a local-only build until then.

**👉 2026-07-23: H4.6 + H4.7 shipped (account deletion + data export)** — the two GDPR items with no dependency on prod or on the H1 IA, so they were buildable during the outage. Settings gained a "Your data" section: a JSON download of everything held about you (tokens excluded, enforced by explicit column lists + a test), and a type-to-confirm account deletion that erases every user-scoped table explicitly rather than trusting `ON DELETE CASCADE`, with the table list derived from the live schema so a future migration can't silently break erasure. **A real session hole surfaced and was fixed on the way:** a deleted account's existing token kept verifying (epoch `?? 0` matched a first-generation token) — deleted users stayed logged in. 307 tests + typecheck + lint + build clean, and rehearsed against a copy of the real DB (4,469 rows erased, 0 of 4 stored credentials in the export, catalog untouched). **Needs your session to eyeball** — but don't run the real deletion on your own account unless you mean it. Details in TASKS.md (H4).

**🔴 2026-07-22: fandex.org is DOWN — Railway paused all deployments at $10.28/$10.00 compute usage.** Not an app fault: every route returns Railway's edge 404. **Your decision: wait for the next billing cycle** (~1 Aug) rather than raise the limit, so the site stays down until then. **No data was at risk.** Ironically the cause is also the cure — the PR16 prune's ~1 h of sustained CPU + ~12.8 GB of WAL churn to S3 tipped usage over the cap, but it also removed the 2.5 GB database that was driving the memory, page-cache and volume costs in the first place, so next cycle should be materially cheaper. Lesson recorded: check Railway usage as a precondition before any heavy prod operation — the "nearing limit" warning was visible beforehand and got treated as an aside.

**👉 2026-07-22: the catalog-pool blowup is FIXED end to end (PR13–PR16 done).** Growth stopped (anon/crawler traffic no longer mints rows) **and** the accumulated tail is gone: **546,754 rows pruned + VACUUM took `rr.db` from 2,487 MB → 36.5 MB**, `media_items` 680,766 → **2,012**, volume free 2.1 GB → 4.18 GB. User data verified untouched on every single pass (`user_library` 1912, `user_watchlist` 96, zero orphans). **Only PR17 (verify + close out) remains, and it's blocked until the service is back** — including confirming the Litestream backup generation survived the VACUUM, which is currently **unverified**. Full detail in TASKS.md.

**Previously: 2026-07-22: catalog-pool growth STOPPED (PR13–PR15 shipped); the 2.5 GB prune (PR16) still needs a dedicated Opus session.** Sitemap now excludes the browsed tail (2,518 pool URLs vs 3,848 total locally), and public facet pages + `/api/discover` only write a `media_items` row for a logged-in viewer — anon/crawler traffic renders "not yet in the catalog" instead of minting rows, browser-verified with a before/after row count. 277 tests pass, typecheck + lint + build clean. **Not yet deployed — needs your push.** Once it's live in prod for a while, PR16 (prune the existing 675k-row tail + VACUUM) is next — do NOT delegate that one, it's a real DELETE against production data.

**Previously (same day): memory ramp returned a THIRD time — diagnosed, root cause is the DB, plan locked (PR13–PR17).** Not a leak this time: Node sat flat at 354 MB RSS / 100 MB heap and Litestream at 58 MB while `cgroup.fileMb` (kernel **page cache**) went 2 MB → 488 MB in 41 minutes. The container is caching pages of a **2.5 GB `rr.db`** — reclaimable, won't OOM (limit 7.6 GB), purely a **cost** problem. Root cause: **675,787 `media_items`** against **1,912 library rows** — public facet pages persist every provider-sourced title (60/page) and crawlers walk them, so the pool grew unbounded. `freelistCount: 0`, so VACUUM alone reclaims nothing. **Found in passing: the sitemap enumerates all 676k items** — ~135 MB and 13.5× Google's 50k-URL cap, so P13b indexing may have been silently dead for a while. Two new diagnostics shipped (`/api/health` now reports cgroup + per-process + DB-file sizes; new admin-gated `/api/dev/dbsize`). Decisions locked: persist only for logged-in users, prune + VACUUM, **PR13–PR15 safe for Sonnet, PR16 (the prune) stays Opus/main-loop** per AGENTS.md. Full plan in TASKS.md ("Catalog-pool blowup + memory ramp — 2026-07-22").

**Previously: 2026-07-21: memory ramp returned — different cause, fixed (PR10–PR12).** RSS hit 7.52 GB the day after the PR1–PR9 deploy, with the error log now entirely image-optimizer timeouts. The heap cap was fine; the memory was **native**: `/_next/image` was decoding **full-size RAWG originals** (726/726 game posters are stored unresized, up to 3.8 MB → ~25 MB decoded each, up to 9 per game page, no concurrency limit, 0.4 vCPU). Fix: images now bypass the optimizer entirely and the CDNs resize instead (custom `next/image` loader) + `MALLOC_ARENA_MAX=2` + `/api/health` now reports the heap-vs-native memory split so the next one takes minutes, not hours. **Accepted tradeoff:** movie/show posters ~26 KB WebP → ~65 KB JPEG; game art gets far lighter; image egress leaves the Railway bill. Detail in TASKS.md ("Production incident — 2026-07-21"). **Not yet deployed — needs your push.**

**Previously: 2026-07-20: Railway incident fixed (PR1–PR8).** The post-P13b crawler wave exposed a lock-error wall (`database is locked`), a memory ramp toward 8 GB, and a $19.54/mo cost estimate. Root causes fixed same day: `BEGIN IMMEDIATE` transactions (Litestream contention), heap cap, bounded caches on the OMDB/item/facet public read paths, image-optimizer hardening, `?sort=` crawl disallow. Full detail in TASKS.md ("Production incident — 2026-07-20"). **Watch the Railway dashboard over the next days**: errors → ~0, memory ≤ ~2 GB, cost drifting back toward $5–8.

**Previously: round-2 QA batch (Q23–Q31) shipped 2026-07-19.** All 9 follow-up items implemented: facet-page popularity/Fandex-badge/`/studio/focus` fixes, Discover tag search + IGDB + Fandex-sort scroll fixes, tag "Fandex impact" pill replacing the Bayesian stat, capped-facet graying, main-vs-support cast weighting, Insights category overrides. 255 tests + typecheck + lint (0 errors) clean; anon surfaces browser-verified. **Logged-in verification still outstanding** — please eyeball the Fandex Score breakdown, facet-page badges, the tag-page impact pill, and Discover's Fandex-sort scroll with your own session (see TASKS.md for the full list). Everything else is either waiting on you (Android TWA, H3.0, legal advice) or not yet scoped (H1).

---

## 🟢 Live
Fandex is live at **https://fandex.org** and ready to share — hosted on Railway (Cloudflare DNS, HTTPS, email routing), all launch-blockers cleared, security hardened, library complete. Phases 1–6 essentially done.

## ▶ What's left
| | Item | |
|--|------|--|
| 🔵 | **Android TWA** (P15/P16) | Needs you to build the TWA (Bubblewrap/PWABuilder) → package name + cert → set 2 env vars. Serving infra ready. |

## 🗺️ Roadmap
| Area | Status |
|------|:--|
| Hosting + deploy (Railway) | ✅ |
| Domain + OAuth + email (fandex.org) | ✅ |
| Backups (Litestream → Railway bucket) | ✅ |
| Observability (`/api/health`, structured logs) | ✅ |
| Security (S1–S13, CSP enforced) | ✅ |
| Sync completeness + TMDB enrichment | ✅ |
| Android TWA | 🔵 needs TWA build |
| SEO SSR detail pages (P13) | ✅ **fully live** — indexing turned on 2026-07-19 (P13b) |
| Public facet pages (P17) | ✅ **done**, live on fandex.org |
| **Post-launch (future):** | |
| UI/UX overhaul — mobile-first polish (H1) | ✅ **DONE 2026-07-27 — H1.0 through H1.6f all complete.** Direction 2a "Ticket · Calm" is fully implemented: tokens/fonts/icons (a), the primitive kit (b), the adaptive `AppNav` + IA restructure (c), content components incl. the new `Rail` and calendar Agenda view (d), all 10 pages/surfaces (e), and the cross-cutting a11y/QA pass (f). The IA restructure shipped in full — public Home at `/`, `/dashboard`→308→`/wishlist`, new `/calendar` + `/profile`, one adaptive nav (desktop top bar / mobile bottom bar), Library⇄Wishlist tab. Design system + per-phase changelog: [docs/ui-overhaul.md](docs/ui-overhaul.md) §10; handoff bundle at [docs/design/fandex-handoff/](docs/design/fandex-handoff/). **Still needs your logged-in eyeball** — every sweep so far has been anon-only (cookie-mint blocked); `smoketest.md` §C lists what to check. Open polish: SM9 (tab-title inconsistency on `/`) + `ActionCells` tap targets. **Not deployed** — Railway was paused until ~1 Aug, so this whole overhaul is local-only so far. |
| Data-model hardening (H2) | ✅ **done** |
| Monetization strategy (H3) | 🟢 **scoped, v1 launch = donations + affiliate only** (2026-07-18): ads + one-time unlock + freemium **deferred to Path B** (H3.8 user threshold); on free TMDB/Trakt tiers meanwhile (risk accepted) · ⚠️ makes H4.0/H4.2 (Impressum) critical path even at this reduced scope |
| Legal & compliance — privacy, cookies, account deletion, support (H4) | 🟢 **scoped** (2026-07-18, ~110k now) · **H4.6 (account deletion) + H4.7 (data export) ✅ done 2026-07-23** — live in Settings' new "Your data" section, not yet eyeballed logged-in · legal links via /profile footer · **Impressum + address deferred to H3 gate** pending your legal advice (H4.0) |
| Fandex Score — visible per-item taste match (H5) | 🔵 **H5.1–H5.4 + H5.6 + H5.7 done** (2026-07-19) — score badge + breakdown are LIVE; `/dev/scoring` weights/taxonomy admin panel (gated to your userId locally — **add `SCORING_ADMIN_USER_IDS` on Railway too if you want it in prod**). **H5.6**: calibration knobs self-explain + Preview pins up to 3 items; **tag bundling** (merge synonym/misspelled spellings into one canonical tag; migration 10). **H5.7**: **unified sort model** — Discover/Library/Wishlist + facet pages now all share Release date · Popularity · Rating (Bayesian) · **Fandex Score**. Nothing here manually checked logged-in yet (needs your own login). Please sanity-check cards/detail, `/dev/scoring`, and the new sort options. Design in [docs/fandex-score.md](docs/fandex-score.md), only H5.5 (calibrate) remains |

---
_✅ done · 🔵 in progress / needs input · 🟢 later · 🔭 future / not yet scoped · 🔒 security · 🔧 config_
