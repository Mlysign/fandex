<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Project doc map (start here)

**Two files carry the live state; everything else is reference or history.**

- `STATUS.md` — current state in one screen: what's broken, what's open, what's blocked on Nils. **Read this first.**
- `TASKS.md` — the open work in detail (source of truth for execution). Opens with a **"Needs Nils"** list — the only things actually blocking progress. ⚠️ **Only OPEN work lives there.** A decision or a constraint has no next action, so it goes in the file below; both kept accumulating in `TASKS.md` and crowding out the work.
- `docs/decisions.md` — **settled calls and standing constraints. Read before proposing anything that sounds like a fresh idea.** Every line is closed; re-litigating one is the waste this file exists to stop.
- `docs/archive/history.md` — everything finished. **Grep it for a keyword** when you need the "why" behind a past decision; never read it end to end and never pull it into context for planning.
- `PLATFORMS.md` — platform integration capability reference.
- `smoketest.md` — the `/smoketest` living plan; findings land in `TASKS.md`.
- `docs/` — live reference only: `fandex-score.md` (H5 design), `monetization-go-live.md` (**the H3 flip-the-switch runbook**) + `monetization-legal.md` + `cookie-assessment.md` + `compliance-review.md` (H3/H4 legal), `design/fandex-handoff/` (the design system), `scalability.md` (**measured provider-call cost per surface; read before adding any provider or promising any scale**), `optimization-plan.md` (**the 2026-08-23 whole-project sweep: measured before/after, plus the ranked list of what is still worth doing and what to leave alone**), `advanced-filters.md` (**the Discover/MyStuff filter sheet: why it rendered clipped (a zero-valued Tailwind `translate` creating a containing block), what the platform filter cost, and the Settings → Your platforms survey — read before touching `FilterPanel.tsx`, `Sheet.tsx` or `userPlatformSurvey.ts`**), `catalog-growth.md` (**the five catalog jobs that now run on a timer: the env switches, what to watch in `/api/health`, and the open IGDB licence question — read before touching any background job or raising `BACKFILL_PAGES`**), `seo.md` (**organic reach: what a crawler actually sees, and the Search Console setup Nils still owes**), `letterboxd-import.md` (**PL4’s design: why the username import is closed by their terms, why Android is a silent dead end, and the import-before-signup rules**). **Performance work is closed** — the audit is archived; `scripts/perf-probe.mjs` + `scripts/probe-pool.mjs` are the starting point for any future pass, and **measure before optimising** (§A was mis-sized by 100× once).
- `.claude/plans/` — executed session plans. **Forensic use only** — don't read one to find out what's open; that's `TASKS.md`.

**Doc conventions. Compression passes on 2026-08-02 and 2026-08-26 restored these; please hold the line.** Four rules, each of which a pass has had to re-apply:

1. **A section that is fully done moves to the archive the SAME SESSION**, leaving at most a one-line pointer. A section reading `✅ ALL DONE, archived` is not a pointer.
2. **STATUS.md holds *state*, not a session-by-session changelog.** That is what git log and the archive are for. It reached 37 KB of dated narrative once.
3. **An invariant in this file is the RULE, in one to three lines, plus a `→` pointer to its memory file.** The incident behind it (measured numbers, why nothing caught it, what was tried and failed) belongs in that memory file. This file is loaded on EVERY session, so the narrative is not free; it reached 56 KB of write-ups duplicating memory once.
4. **An index entry is a hook**: enough to answer "do I need to open this?", and nothing more. That applies to the doc map above and to the memory index.

**Budget, measured at the top of any hygiene pass:** always-loaded (`CLAUDE.md` + this file + the memory index) under ~25 KB; orientation (plus STATUS.md + TASKS.md) under ~50 KB. CI warns when `TASKS.md` passes 200 lines; it has blown past it twice.

⚠️ **The ~25 KB figure above is WRONG and has been measured to be unreachable (2026-09-02). Do not chase it.** Two compression passes took this file 56.1 → 52.7 KB with all **93 invariants and 40 `⚠️` sub-rules** intact, and that is where careful rewriting tops out: the remaining bullets average **441 bytes**, which is the rule plus its sub-rules plus a pointer and almost no narrative left to cut. Getting to 25 KB would leave ~55 bytes per rule — one sentence, no pointer, no sub-rules — so it means **deleting invariants, not compressing prose**.

**The byte budget was measuring the wrong thing.** This file's size is a function of how many rules it holds, not how verbosely it holds them, and the rule count grew from ~40 to 93 while the number stayed put. The question worth asking of this file is not "is it under 25 KB" but **"has every rule in it earned its place, and is each one the RULE rather than the story?"** — and the story belongs in the linked memory file.

**So: ~55 KB is the honest ceiling for the always-loaded set.** Nils delegated this on 2026-09-02 ("compress agents.md the way you think is right"), and the call was to keep every invariant inline rather than move a category behind a pointer. These rules exist *because* somebody did not know they were touching that subsystem; a pointer only helps a reader who already worked that out, which is the one reader who did not need it.

## Stack + commands

Next.js 16 (App Router, Turbopack) · React 19 · TypeScript · Tailwind v4 · SQLite via `better-sqlite3` (synchronous, in-process, single file → **one always-on process, not serverless, not multi-instance**) · JWT sessions via `jose` · Vitest.

| Task | Command |
|---|---|
| Dev server | `npm run dev` (:3000) — **never run `next build` while this is running** |
| Tests | `npm test` · `npm run test:watch` |
| Typecheck | `npx tsc --noEmit` |
| Lint | `npm run lint` — 0 errors is the standing bar; warnings are `no-explicit-any` noise |
| Build | `npm run build` |
| Migrate a live DB | `node scripts/migrate.mjs` (the standalone path — see the invariant below) |

Env vars: the authoritative table is in `README.md`; `src/lib/config.ts` fail-fasts at boot on the five required ones.

## Repo map

- `src/app/` — routes. `api/` holds 64 route handlers (`account auth calendar csp-report detail dev discover episodes facet health home import insights library progress settings sync telemetry watchlist`); the rest are pages. Root-level `[type]/`, `person/`, `tag/`, `studio/` are the PUBLIC SEO surfaces (P13/P17) — a new root-level dynamic segment breaks a lint rule that gates CI. `r/[source]/[type]/[id]` (2026-08-23) is NOT one of them: it is a `noindex` + robots-disallowed resolver that ingests one provider title on demand and 307s to its real page, and it is what makes a franchise member we do not hold clickable. The item page is `[type]/[slug]/page.tsx`; `[type]/[slug]/[legacy]/page.tsx` is the pre-2026-08-21 uuid url and only redirects.
- `src/lib/` — 170 modules + 116 colocated `*.test.ts`, mostly flat. The load-bearing ones: `db.ts`/`migrations.ts` (schema), `matcher.ts` (identity + write paths), `merge.ts`/`facets.ts` (projection), `discovery.ts` (catalog pool, Fandex Score, ranking), `liveDiscover.ts` (provider-fed browse feed), `libraryAnalysis.ts` (taste profile), `session.ts` (auth), `http.ts` (every third-party call), `affiliate.ts` (monetization — off by default), `episodes.ts` (MB14 per-episode tracking: catalog fill + the provider reconcile that inherits the prune invariant), `upNext.ts` (Home's progress rail), `franchiseMembers.ts`/`franchiseSweep.ts` (what a franchise ACTUALLY contains, as thin rows), `detail/relatedRails.ts` (the item page's two rails, shared by the page's server render and `/api/detail/similar`).
  - `sources/` — per-provider fetch + normalize + project; `sources/adapters/` are the sync pull/push adapters (**the prune invariant lives here**).
  - `sync/index.ts` — `syncProvider`, the one place that prunes.
  - `detail/` — item + facet detail assembly, incl. the public (leak-boundary) variants.
  - `metadata/`, `legal/` — provider registry, and the bilingual legal content tree.
- `src/components/` — 90 components; grouped subfolders are `auth discovery facet insights item legal ui`, plus `dev` (debug-only surfaces).
- `android-package/` — **gitignored, and must stay that way.** The PWABuilder output for the Android TWA (P15/P16): `Fandex.aab`, `Fandex.apk`, plus `signing.keystore` (the Play **upload** key) and `signing-key-info.txt`, which holds its passwords in **plaintext**. It lives here rather than in Downloads so OneDrive backs it up; it must never reach the remote. → `docs/twa-play-store.md`
- `scripts/` — standalone Node tooling (migrate, rehearsals, probes). These run under plain `node` via `alias-hooks.mjs`, which is why the `import type` rule below is load-bearing.

## Verifying anything behind a login

`/insights`, `/profile`, `/settings`, `/library` and `/wishlist` all render a sign-in gate for anonymous visitors (the last two since 2026-08-18, the first three since 2026-08-27), and the item-detail personal block swaps to its anon controls. Either way **the real content can't be checked without a session.** Use **`GET /api/dev/login`** — it mints one for the `users.id` in `DEV_LOGIN_USER_ID` and redirects to `/`; the local `.env` already points at the real account. Works in the in-app browser pane.

**For anything DESTRUCTIVE, use `GET /api/dev/login?as=smoketest` instead.** The default login is NILS'S account, where rating a movie is a real write-back to Trakt and TMDB that has to be undone by hand; that cost is why nobody ever rated a game and "you can no longer rate games" survived a full session of verification. `node scripts/smoketest-account.mjs data/rr.db` creates it (`--reset` wipes its state, `--status` reports it); its `google` identity is identity-only, so nothing it does leaves the machine. ⚠️ It starts EMPTY, so a cold-start profile scores nothing: use it for WRITES, and the default login for anything needing a real library.

Don't hand-mint a JWT (the harness blocks it as credential-forging, correctly) and don't assume anon-only verification is the best available — that assumption is why several batches shipped "not yet verified logged-in". **Never call `/api/auth/logout` to test the anon side**: it bumps `session_epoch` and kills Nils's own browser session, which only a real OAuth round-trip restores.

The route's three fail-closed gates (`NODE_ENV !== "production"` · loopback host · a real identity row) are what keep it off fandex.org. Do not relax them, and treat it as auth code — main-loop only, per the routing rules below.

## Load-bearing invariants (don't relearn these the hard way)

**Each line is the RULE. The incident that produced it (measured numbers, why nothing caught it, how it was found) is in the linked memory file, which you read BEFORE touching that subsystem.** Compressed 2026-08-26; nothing was dropped, only moved.

### Data, sync, and the database

- **The prune invariant: a sync pull that fails must THROW, never return `[]`/partial.** `syncProvider` (`src/lib/sync/index.ts`) deletes every local entry missing from a successful pull, so a swallowed error wipes the user's library. A pull that legitimately found nothing may return `[]`. **Anything a pull carries beyond ids inherits this**, so `undefined` ("this pull says nothing about X") and `[]` ("authoritative, and there is none") must stay distinct. Gate on a declared capability (`capabilities.episodes.read`), never on a sniff of the returned shape. → [[trakt-sync-completeness]]
- **A per-user DISPLAY preference must never reach a sync pull.** `users.platforms` and `users.media_types` (migrations 24/25) filter what a person SEES. `pruneWatchlist`/`pruneLibrary` read "absent from the pull" as "removed upstream", so filtering a type or platform at the pull layer would DELETE every matching row. Turning Games off has to leave the rows exactly where they are. The same rule bars these from `home_snapshot`/`calendar_snapshot` (viewer-independent by contract) and from the Fandex Score (every facet weight is a deviation from the user's GLOBAL baseline, so dropping a type moves the score of everything else). → [[user-display-preferences]]
- **Check sync staleness PER PROVIDER, never with one collapsed `Math.max`.** A Trakt-only run made Steam look fresh and froze it for three weeks. `staleProviders()` in `syncClient.ts` is the only decider; `syncToCompletion()` takes a LIST. ⚠️ An empty list must be an explicit no-op: `/api/sync` falls back to the whole registry on an empty `providers`. → [[trakt-sync-completeness]]
- **An OAuth callback that finds an EXISTING identity must check who owns it.** `handleOAuthCallback` had one unconditional token UPDATE, so connecting a provider already linked to another account refreshed THAT account's row, linked nothing, and still redirected to `?connected=` — the page said "connected successfully" next to an unchanged Connect button. Three outcomes, not one: no session → log in as the owner; owner is this session → refresh; owner is someone else → merge or refuse, never silently no-op. ⚠️ A merge switches which user the session is for, so it must set a NEW session cookie (the old branch only did that for a fresh login) and it must move rows via the schema-derived `userScopedTables()`, the same list erasure uses. ⚠️ Refuse rather than invent a conflict rule when both accounts hold per-item state — `user_item_state` is unique on `(user_id, media_item_id, source, relation)`, so a blind move collides and `OR IGNORE` would silently drop somebody's ratings. → `src/lib/accountMerge.ts`
- **An identity provider that holds no library must be listed in `IDENTITY_ONLY_PROVIDERS` (`syncClient.ts`).** `staleProviders()` reads "no `sync_log` row" as "overdue", which is right for a provider that CAN sync and permanently wrong for one that cannot: it reads as due forever and fires a doomed sync on every `/library` load, silently. ⚠️ **`AuthProvider` (can log you in) is a SUPERSET of `Source` (a place media comes from)** and the two must not be collapsed: putting `google` in `Source` would make it legal for a store link, a release date and an `/api/sync` target. → [[identity-only-auth-providers]]
- **Migrations have TWO apply paths**: in-process via `getDb()` (tests + prod) and standalone via `node scripts/migrate.mjs` (plain Node; resolves neither the `@/*` alias nor extensionless specifiers). Green tests only prove the first. After touching `migrations.ts`, run the standalone script against a DB copy. → [[db-migrations-and-testing]]
- **`db.ts`'s `CREATE TABLE`/`CREATE INDEX` block runs BEFORE migrations** and must stay valid against the OLD schema. A column added by a migration gets its index created in that *same* migration. Putting them together works on a fresh DB and silently breaks every upgrade path.
- **Any SQL reaching the SCHEMA must parse on the SQLite LITESTREAM embeds (~3.40), not the 3.53 `better-sqlite3` ships.** SQLite parses the whole schema before preparing any statement, so one unsupported construct stops the backup daemon dead while every test, the build and `/api/health` stay green. Express sorting as `ORDER BY` in a SUBQUERY, never inside an aggregate's argument list. Check new schema SQL with an OLD `sqlite3` CLI against a DB copy, and re-run the restore drill after any schema change. → [[prod-incidents]]
- **`user_library` / `user_watchlist` are VIEWS**, and a `CREATE INDEX` on either kills the next boot. → [[cache-tables-are-views]]
- **A new table referencing `media_items` must be added to `dbPrune.ts`'s `PRUNABLE_WHERE`.** The boot prune is default ON in prod and deletes `browsed = 1` rows nobody "acted on", where acted-on is a hard-coded table list. Extend the list; don't reason about whether one table implies another.
- **Any table written on a REQUEST path needs a row or byte ceiling, not just a TTL**, run on an unref'd interval in bounded batches, evicting by WRITE time (tracking reads turns every cache hit into a write). An age cap is not a size cap and a boot-only sweep is not a schedule: prod runs for days, and a crawler keeps every row inside the TTL window while the table grows all day. Deleting rows does not shrink the file, but `db.ts` VACUUMs after any migration applies, so you rarely need to act. → [[prod-incidents]]
- **The thin-write / pool rule:** a discover-time write is insert-only and stamps `browsed=1` / projection version `0`. It must never overwrite or degrade a real synced/ingested row. → [[data-model-gaps-and-plan]]
- **Erasure is schema-derived, and a `user_id` column is what makes a table personal.** `deleteAccount()` finds targets by reading `sqlite_master` for that literal column name. Name it anything else and GDPR erasure silently skips it. A CATALOG table must NOT have one. `/api/account/export` uses explicit column lists, so a new user-scoped table needs a block added there BY HAND. → [[account-erasure-and-export]]
- **A provider id is unique only WITHIN a media type, so `media_links` is keyed `(source, source_id, media_type)`** (migration 23). Trakt and TMDB number movies and shows in SEPARATE sequences: trakt 386 is both *Being John Malkovich* and *SpongeBob SquarePants*. Every lookup that turns a provider id into an item must carry the type; without it the second work to sync overwrites the first's row, merging one work's links, genres and watch history into the other. `findMatchingItem` was always type-filtered, which is why this hid. → [[cross-type-identity-merge]]
- **The pool PATCHES a content change; it must stay EXACTLY equal to a rebuild.** `unfoldEntry` un-counts an item's vocab with the RAW pre-alias facets it was folded with (`_cache.rawFacetsById`); post-alias ones are a different set. A vocab that is merely close is not a small bug — `computeIdf` reads those counts, so every ranking shifts silently. Every check runs BEFORE any mutation, and a throw is answered with a rebuild. ⚠️ The changed set is `updated_at >= prevMx`, never `>`: one-second resolution means `>` misses a write. ⚠️ A DELETION still rebuilds (which row went is unknowable from an aggregate). → `docs/archive/history.md` (catalog growth §16)
- **A same-length payload rewrite inside one second is invisible to the facet cache**, whose token is `(last_synced, OCTET_LENGTH)`. Swapping a nine-byte genre for another nine-byte genre serves the OLD derivation, correctly. Know this before blaming code: it cost an hour once, in a test fixture. Likewise `contentSignature` cannot see a same-second re-sync at all (neither COUNT nor MAX moves) — the 5-minute pool TTL is what covers that, and any test of a re-sync must bump `updated_at` by hand.
- **Before a `PROJECTION_VERSION` bump**, check whether lazy self-heal already covers it. → [[lazy-heal-vs-mass-reprojection]]
- **A helper that signature-checks its cache must be called once per PASS, never once per item.** `computeFandexScore` called three such helpers per item, so **79% of Fandex Score CPU was cache validation**. `scoringContext()` fetches them once: **5.1×**, byte-identical. Any loop over the catalog wants the same. → `scripts/probe-score.mjs`
- **A cache KEY must be cheaper than the thing it guards, and it must never touch a VIEW.** `librarySignature` asked `user_library` (a view of CTEs, `json_group_array` and two GROUP BYs) for a COUNT and a MAX: **69 ms per call, three calls per request**, to produce a cache key. Off the base table, with a covering index, it is **1 ms** — and `find()` went 190 → 86 ms. Sign off `user_item_state`; a per-source count is strictly more sensitive than a per-item one, and a signature only has to CHANGE. → `docs/archive/history.md` (catalog growth §11)
- **An index whose aggregate reads a column must COVER it.** `MAX(ml.last_synced)` through `idx_links_item` fetched every matching `media_links` row, and those carry ~7 KB of `raw_data` each: tens of MB of pages to collect one integer per row. **41.5 → 1.0 ms** with `(media_item_id, last_synced)`.
- **A memory fix sized from `JSON.stringify` length is sized wrong.** `poolWeight()` said the scoring pool was the biggest thing in memory; measured RETAINED heap says it is **13.5 MB of 109.6**, and `facetCache.derived` was 86 MB. The planned facet-string interning, at its theoretical best (130,737 occurrences → 22,341 objects, 5.85× reuse), saved **0.4 MB**. Serialised bytes are a growth RATE, never a target list. Measure with `node --expose-gc scripts/probe-memory.mjs`. → `docs/archive/history.md` (catalog growth §13)
- **Price a cache ENTRY before choosing its bound, and never store a field no caller reads.** `facetCache` stored `sources[].data`, the whole parsed provider blob: **76% of a 25.5 KB entry**, making its `max: 6000` a ~200 MB ceiling. Two routes had been dropping that field on the WIRE for a month while the process held the same bytes. Make the TYPE exclude it so `tsc` is the proof. → `docs/archive/history.md` (catalog growth §13)
- **A token computed in BOTH SQL and JS must be UTF-8 BYTES** (`OCTET_LENGTH` / `Buffer.byteLength`). SQLite's `LENGTH()` counts code points, JS `.length` counts UTF-16 units, so any payload with an emoji made facetCache's freshness key unmatchable: **56 of 7,006 links never once hit that cache**, invisibly, since a permanent miss only costs time. `LENGTH()` on TEXT also has to decode the string — **67–84 → 8–12 ms** over the pool. → `facetCacheFreshnessToken.test.ts`
- **A cache key that encodes a WRITE-mode flag must be filled for BOTH modes by the writing build.** `publicFacetDetail`'s key carries `persist`, correctly (it changes which items are linkable), so the two flags own separate entries. A build that persisted 419 titles left the `nopersist` entry holding the pre-write payload, and every crawler kept the dead-text version for the full 24 h TTL: the sweep logged `linkable: 419/419` while the anonymous page served 214. Writing the same object under the other key is exact, not an approximation — once the rows exist, `lookupExistingUuids` resolves what `persistDiscoverItems` just wrote. ⚠️ ONE DIRECTION ONLY: a `nopersist` build must never fill the `persist` entry, or a later persisting caller skips the write. → `docs/seo.md`
- **A cached array indexed by POSITION may be served stale, never misaligned.** `fandexScoresFor` serves the previous scores while a chunked pass refreshes them, so no request scores the catalog; that is only safe because `poolSig` is tracked apart from `sig` ("index *i* still means `vectors[i]`" is a different question from "these numbers are current") and a moved pool is REMAPPED by id, with `NaN` → `fandexPending` for anything new. → `docs/archive/history.md` (catalog growth §12)
- **An item that is already a catalog row carries no `raw`, and BOTH uuid resolvers skip those** (`lookupExistingUuids`, `persistDiscoverItems`). `persistDiscoverBatch` passes a uuid `id` through for exactly this reason. Miss it and a whole page renders `linkable: false` — cards that look right and click nowhere. → `docs/archive/history.md` (catalog growth §10), [[anon-surface-has-no-item-links]]

### Providers, caching, and cost

- **Per-source FAILURE isolation is not per-source LATENCY isolation, and the breaker must THROW.** `http.ts` has a per-host circuit breaker plus an optional `budgetMs`; browse paths pass `BROWSE_BUDGET_MS`, sync/pull deliberately keeps its unbounded budget. `ProviderUnavailableError` must never be a fabricated Response: a synthetic 503 reads as `!res.ok` to the pull adapters and turns an outage into a wiped library under the prune invariant. Browse opts into degradation explicitly (`bestEffort`); pull code keeps throwing. → [[provider-latency-isolation]]
- **If a medium has two providers, a single-source pull is a bug.** Games are RAWG *and* IGDB in the BROWSE feed (`fetchGamePageAllSources` / `fetchTrendingGames` / `dedupeGames`, one correct copy; three single-source ones is how they drifted). ⚠️ **The facet paths dropped RAWG entirely and that split is deliberate** — cutting it from browse too was tried and reverted the same day, and `discoverFeedSources.test.ts` fails if you retry it. When touching any games surface check ALL of them: initial browse, load-more (a *different* path), trending, calendar-popular, profile. → [[provider-latency-isolation]]
- **A provider's response SHAPE is not verifiable from its documentation, and a mocked test of an assumed shape proves nothing.** When an integration returns nothing, probe the payload before theorising about the code, and prefer a check the environment can actually run (a call count) over asserting a payload you had to invent. → [[trakt-episode-endpoints]]
- **A provider call carrying only an APP credential must opt into the 401/403 latch with `appScopedAuth: true`.** Five consecutive 401/403s open the host for 15 min, doubling to a 6 h ceiling; any other response clears the run. ⚠️ Opt-in is per CALL SITE, not per host: RAWG and TMDB serve app-key and per-user calls from the SAME host, so counting a user-scoped 401 would let one expired token stop the provider for everybody. `src/lib/httpCallSites.test.ts` fails if an `httpFetch` carries an app key without the flag. → [[provider-quotas-and-scale]]
- **A cache goes through `sharedCache(name, opts)`, NEVER `new BoundedCache()`.** Next resolves a module into a different bundle per route kind, so module-level state is per-bundle, not process-global, which silently broke the circuit breaker for three weeks. `sharedCache` pins by name to `globalThis`; the name IS the identity, so two caches sharing a name share STORAGE. Bounds are ENTRY COUNTS, not bytes (`GET /api/dev/dbsize?caches=1` prices an entry). `vitest.setup.ts` clears them in a global `beforeEach`. It has to, since a pinned cache survives between tests. → [[provider-quotas-and-scale]], `docs/scalability.md` §6
- **The discovery pool must NOT be capped** (`_cache.vectors` in `discovery.ts`, ~2.6 MB per 1,000 catalog items). It is the candidate set, so truncating it silently changes every Fandex Score, "More like this" ranking and IDF weight. Measure it (`poolWeight()`) and plan for the slope. → [[data-model-gaps-and-plan]]
- **Identical WARM and COLD timings on a route mean there is no cache at all**, and that is a cheaper diagnosis than reading the code. Every provider boundary is cached now; if you add one, cache it, pin it to `globalThis`, and **never cache an empty result**, which pins an outage in place for the whole TTL. → [[browse-page-cache-and-crawl-dead-ends]]
- **Provider cost scales with CATALOG BREADTH × CRAWLER APPETITE, not pageviews, and the quota is the ceiling.** Price any new provider call on a crawlable page in cold-facet-page units via `/api/health`'s `providerCalls`. **OMDb is DELETED** (CC BY-NC at every tier; `noOmdb.test.ts` pins it out, so do not re-add) and **RAWG is off the facet paths**. ⚠️ A cold `/tag/` page should now cost 10 requests, none RAWG. That is a PREDICTION, not a measurement. → [[provider-quotas-and-scale]], `docs/scalability.md`
- **`openProviderCircuits: {}` is not a health check.** → [[facet-cache-and-provider-latency-masking]]
- **Retiring a provider means auditing every hard-coded `Source[]`, not just `SOURCES` and `METADATA`.** A bare array that omits a source silently SKIPS it, and `Source[]` is satisfied by a list naming two of six, so tsc sees nothing. `persistItem.ts`'s id-precedence list omitted `igdb`, so after the RAWG retirement every IGDB-only game resolved to null and both quick-action routes answered `400`: rating and wishlisting were dead for 310 of 1,347 games. `persistItemSources.test.ts` holds that one list to the full union. → [[provider-retirement-hardcoded-lists]]
- **IGDB runs behind a KILL SWITCH: `IGDB_ENABLED=0` stops every call, default ON.** Its licence question is open, so the decision was to continue and make a "no" cheap. `igdbKillSwitch.test.ts` fails any export in `sources/igdb.ts` reaching `igdbQuery` without the guard. `scripts/purge-igdb.mjs` removes what is stored: links, never items. ⚠️ **Games are now IGDB-ONLY** (RAWG retired 2026-09-02), so flipping this switch takes games' browse to nothing rather than halving it. ⚠️ Default-ON is correct: a typo must leave the site working. → `docs/catalog-growth.md`
- **A WAL-mode database is the `.db` PLUS its `-wal`.** Copying only the `.db` gives a pre-WAL snapshot that reads as perfectly valid and is silently stale: it reported 1,580 projection rows against the 908 actually there, because a DELETE was still in the WAL. Copy both, or every count you take is a guess. This is also why `scripts/*` probes say to point at a COPY and mean a complete one.
- **A SAFETY GATE read at module load is a gate nothing tests.** Three shipped that way in one session (the backfill ceiling, the housekeeping threshold, the browse minimum) and all three had a test silently asserting the DEFAULT instead of the behaviour, because setting the env var in a test does nothing after the module is loaded. Read a threshold or a switch at CALL time — `backfillMaxItems()`, `housekeepingStartMb()`, `catalogBrowseMin()`, `igdbConfigured()` are the shape.
- **A background job's EMPTY answer is not the same as "finished", when the fetcher swallows failures.** Count consecutive empties (three) and reset on any real page. Same `undefined` vs `[]` distinction as the prune invariant; the blast radius is a lane that silently never runs again. ⚠️ **Three strikes raises the COST of a false retirement, it does not prevent one** — a quota latch is not transient, so read `exhausted: true` on a latched provider as "provider down". ⚠️ **A retired lane cannot currently be revived**: `resetBackfill()` is exported and wired to nothing. → `docs/catalog-growth.md`
- **A catalog-served card must carry every field the client SORTS on, or the control silently reorders nothing.** `voteCount: 0` becomes `communityVotes: 0`, every row ties, and a stable sort shows ARRIVAL order under a control labelled "Popularity" (the 2026-08-29 search bug again). `media_items.vote_count` / `vote_average` are denormalised for this by the SAME functions the search path uses, so the two cannot disagree. ⚠️ **This app's "popularity" IS the vote count**; do not add a provider trending metric as a second one. ⚠️ `averageCommunity` is 0-100, `voteAverage` is 0-10. ⚠️ **`ORDER BY vote_count DESC` alone pages the window BACKWARDS on a fresh DB** (SQLite sorts NULL lowest and "not computed" is NULL); lead with `vote_count IS NULL`. → `src/lib/itemStats.ts`
- **The stats refresh runs BEFORE housekeeping, and that ordering is a correctness requirement.** Housekeeping reclaims `media_links.raw_data`, the only place the votes exist, so an item blob-dropped first would sit at a stamped permanent 0. It is off the write path for a separate reason: `lookupExistingUuids` short-circuits known items into a plain SELECT so browse takes no write lock (PR13-PR15).
- **A window defined RELATIVE TO NOW cannot be filled by a one-shot sweep, because it drains while you fill it.** The backfill's `:future` lanes target the next 18 months; every lane exhausted and the counts then FELL (movie 49 → 35, show 49 → 26 in four days) as release dates passed out of the window. A target that moves needs a repeating job, not a cursor that finishes. Check whether a count is a stock or a flow before promising it will reach a threshold. → `docs/catalog-growth.md`
- **`strftime('%s','now')` has ONE-SECOND resolution, so it cannot order things within a pass.** Every backfill lane a pass touched shared one second, so "least recently run" could not separate them and one lane was hammered while five starved. Track what a pass has already used, and tie-break on DECLARATION order rather than alphabetically (which quietly re-prioritises the list). The same resolution is why `contentSignature` cannot see a same-second re-sync and why facetCache needs a length in its token.
- **Backticks inside `db.ts`'s schema template literal END THE STRING.** A SQL `--` comment written with ``identifiers`` is a parse error at the next quote, and the error points at the top of the block, not at your line. Cost two debugging rounds in one day. Write schema comments in plain prose.
- **A provider's RETENTION cap is a separate question from its commercial tier.** TMDB caps caching at **six months** (API Terms §1.C): an AGE cap, not a size one, so a stored catalog is fine and an unrefreshed one is not. `healLinks` re-fetches on PROJECTION VERSION and never on age, so `lib/retention.ts` marks ageing links un-projected instead. ⚠️ **IGDB is unresolved and enforces nothing on purpose**: the Twitch DSA allows a 24-hour cache or prior written authorization, which its own webhooks product contradicts. Do not pick a number. → `docs/catalog-growth.md`
- **Check a provider's USAGE TERMS before its capabilities**, and treat a `PLATFORMS.md` capability cell as a claim about code. Two of them were wrong. → [[platform-integration-architecture]]

### Routes, URLs, and SEO

- **A public item url is `/{type}/{slug}`, off a STORED, IMMUTABLE, per-type-unique slug.** Assigned once by `ensureItemSlug` (matcher.ts's create path, AFTER `remergeItem`) and never changed. The uuid is a ROW id and the boot prune deletes rows, so a uuid url 404s after a re-open. The legacy 3-segment route must stay (it 308s, and every shared link points at it). Collisions are real (40 of 2,530 within a type): first-come keeps the bare slug, the newcomer takes `-{year}`. Thread `slug` into any new card payload. `publicItemHref` otherwise falls back to the legacy url and costs a 308 per click. → [[item-urls-are-slugs]]
- **Any route reading an env var to build an absolute URL must export `dynamic = "force-dynamic"`.** Next prerenders route handlers without it at build time, and Railway's build-phase env differs from runtime, so the wrong value is baked in permanently, status 200 either way. Verify in `npm run build`'s route table: `ƒ (Dynamic)`, not `○ (Static)`. → [[railway-build-vs-runtime-env]]
- **Any `NEXT_PUBLIC_*` read by a CLIENT component needs a Dockerfile `ARG`**. Server components still render it fine, so it looks like it worked. ⚠️ **The ARG is only half: the VALUE must exist on Railway BEFORE the build that bakes it, and setting one afterwards needs a REBUILD, not a restart.** Next inlines `NEXT_PUBLIC_*` as a literal at build time on the server side too, so a var added after the build reads `undefined` in the running container no matter how many times it restarts — and `railway variable set` does not itself rebuild. Adding `NEXT_PUBLIC_DISCORD_CLIENT_ID` eleven minutes after the deploy left `/api/auth/discord` 404ing on a container whose code was correct. Set the variable FIRST, then deploy. → [[next-public-env-needs-dockerfile-arg]]
- **A client island fed by `/api/` is invisible to Google, not "probably seen":** `/api/` is under the robots `Disallow`, and Googlebot's renderer honours robots.txt for SUBRESOURCES. The fix is to pass the data in as PROPS, not to make it a server component. A client component's first render already IS server HTML. → [[home-snapshot-and-crawlable-rails]], `docs/seo.md`
- **Each daily snapshot has rules that are the shape of an incident already on file.** A failed build must never replace a good one (atomic replace AFTER a validated build; clearing first turns an outage into an empty homepage). `home_snapshot_item` is in `PRUNABLE_WHERE`, and the refresh is sequenced AFTER `bootPrune` resolves. ⚠️ `home_snapshot` is bounded by its PK; **`calendar_snapshot` is NOT** (keyed by region AND month over a SLIDING window), so the build must delete out-of-window months and out-of-use regions. ⚠️ Its window is **−5..+6, not ±5**, and a test asserts it stays a superset of `indexableMonths()`. → [[home-snapshot-and-crawlable-rails]]
- **When a route threads a per-user parameter into a cache key, the pre-build must cover the values that parameter actually takes.** A snapshot built for `DEFAULT_COUNTRY` only passed every test and fixed nobody signed in outside it. `snapshotRegions()` reads `users.country`, capped at 8 with a log line when the cap bites. **Measure BOTH auth states.** → [[home-snapshot-and-crawlable-rails]]
- **Five SEO decisions that read as tidy-able and are not.** `aggregateRating` stays OUT of the item-page JSON-LD (every number we hold is someone else's; publishing it earns a SITEWIDE manual action). `robots.ts` needs BOTH `/calendar/` (allow) and `/calendar` (disallow) — longest match wins, so the SSR month pages are crawlable and the client-rendered app page is not. `/calendar/[month]` bounds its crawl surface three ways, each load-bearing. ⚠️ **A CAP APPLIED AFTER A SORT IS A SILENT FILTER**: an alphabetical genre list capped at 24 cut every game genre from a catalog that is a third games. A thin-page rule must test POOL SIZE, not LINKABLE COUNT. → [[seo-and-organic-reach]], `docs/seo.md`
- **The legal surface is CLOSED and filled.** `/legal/{en,de}/{privacy,terms,support,imprint}` all carry real content; the Impressum holds a real home address and stays `noindex, nofollow, noarchive, nosnippet` and out of `sitemap.ts`. Do not add it to the sitemap, do not relax the robots directives, and do not "restore" an EU ODR/OS-Plattform link, because that platform shut down 2025-07-20.
- **Monetization is off by default and that is the legal gate, not a preference.** `MONETIZATION_ENABLED` (unset = off) makes every affiliate link inert; the first live one makes Fandex commercial under §5 DDG. Decoration runs ONLY at the two detail boundaries via `decorateItemLinks()`, never in `normalize.ts` (needs a `PROJECTION_VERSION` bump) and never in `mergeLinks()`. Every link stays a direct outbound `<a href>`: a Fandex-hosted redirect or click pixel breaks §25 TDDDG's cookie exemption *and* Amazon's terms. → [[monetization-h3]], `docs/decisions.md`
- **A text search must never skip the providers because the local answer "looks good".** Any bar judges completeness from our OWN rows, and a title is a prefix of its own sequels: an exact match on "Blade Runner" hides *2049*, "Cars" hides *Cars 3*. Serve the catalog first and fast (`find()` takes `userId: string | null` for exactly this), keep the provider half, and make it cheap with a cache — `discover.search` was the last uncached provider boundary in the app. → `docs/archive/history.md` (catalog growth §14)
- **A list whose sort key half its items do not carry is not sorted — it is served in ARRIVAL order.** One feed skipped `decorateSection`, so every result carried `communityVotes ?? 0` and a stable sort showed ascending DATE order under a control labelled "Popularity". ⚠️ Its sibling: a dedupe keyed on title+type with NO YEAR drops the second work sharing a name, and oldest-first arrival makes that always the NEW one — so search reads as "we don't have it". → `docs/archive/history.md` (grep "could not find a new title")
- **Counters, not events, and no `user_id`, on purpose.** A client beacon does NOT exclude crawlers. → [[telemetry-self-hosted]]
- **The pageview beacon gates on a request SHAPE, not on a user-agent name.** A UA list only catches what somebody thought to name: prod took 9,917 views on 2026-09-02 (9,532 of them facet and item pages against 42 homepage views) with 15 blocked. `isSameOriginBeacon` requires `Origin` or `Sec-Fetch-Site` to agree with the host, which no scripted client sends whatever UA it claims. ⚠️ Requiring only ONE marker is the precision half (pre-16.4 Safari sends no `Sec-Fetch-Site`), and it compares HOSTS, because Railway terminates TLS so the request reads `http:` while the browser sent `https:`. ⚠️ It cannot catch a real headless Chromium; `TELEMETRY_AGENT_SAMPLE` logs a 1-in-20 UA sample to name that one. → `telemetryBeaconShape.test.ts`
- **"Which surfaces does a display preference reach" is a PRODUCT question, and the honest default is every surface that CHOOSES on the viewer's behalf.** Hiding a title was scoped to the recommendation rail only, reasoning that trending and upcoming come out of the viewer-independent snapshot; Nils hid a show and it stayed in "Popular right now". That reasoning read the implementation instead of the screen. Filter in the PER-USER OVERLAY (`withUserOverlay`), which keeps the snapshot built-for-nobody and crawler-cheap. ⚠️ When the preference must also stay REACHABLE, gate on the search term, not on the surface: the Progress tab is served hidden rows flagged and drops them unless `f.q` is set, so "not in the feed" and "findable by name" are both true. → [[hidden-items-and-destructive-testing]]
- **A new user-scoped table has THREE obligations and only one of them is automatic.** Erasure is schema-derived and free (a literal `user_id` column). The export is written by hand (explicit column lists) and `dbPrune.ts`'s `PRUNABLE_WHERE` needs the table by name, or hiding/marking a `browsed = 1` row UNDOES ITSELF on the next deploy: the prune deletes the item, the cascade takes the preference, and the title walks back. Walked end to end for `user_hidden_items`. → `src/lib/hiddenItems.ts`

### UI, layout, and the browser pane

- **An auth gate must ASK, never disappear.** A gate that redirects, returns `null`, or renders inert "sign in to…" text is indistinguishable from a broken feature. Before gating, ask which part is actually per-user; half of one batch of six were gating the wrapper rather than the data. ⚠️ `withOptionalUser` hands the handler `session | null` — **pass the null through, never a placeholder id** (it is `persistDiscoverBatch`'s PR15 write gate). Size `anonLimit` to what the route SPENDS. Shared gate: `components/auth/SignInGate.tsx`. → [[anon-gates-must-ask-not-bounce]]
- **React's synthetic `onMouseEnter`/`onMouseLeave` follow the REACT tree, so a PORTAL breaks them permanently.** A portalled child (ActionCells' star picker) is a React descendant wherever it renders, so moving the pointer into it is not a "leave"; when it then unmounts under the cursor React tracks a detached node and that element never receives another leave event. The tooltip stayed pinned open forever. Track hover with NATIVE listeners on the element, which are computed from its own DOM box. ⚠️ Diagnose by attaching both kinds at once: native enter 1 / native leave 1 / tooltip still open is the whole proof. → `posterCardHoverListeners.test.ts`
- **A detail surface gets the value its parent DERIVED, never the raw prop it derived it from.** PosterCard resolves a `fandexPending` card's score client-side and handed the untouched `item` to the explainer, whose layout branches on `fandexScore != null`: the card read 78 while the tooltip showed a date and a type chip and never fetched the breakdown. Both objects satisfy the prop type and both render, so only a person looking at it catches it. `posterCardTooltipScore.test.ts` pins both surfaces. → [[card-list-components]]
- **A component that renders `null` when empty must know WHY it is empty before it ships.** "No data yet", "the pull is FAILING", "still filling a cache" and "genuinely caught up" all read as "the feature doesn't work". Ship a `*Status`/`*Diagnostic` returning the counts that separate them, computed independently so a throw in the main path can't take the explanation down too.
- **When ONE component is fed by TWO routes, every field the shared code reads must arrive from BOTH.** The missing half fails silently, as an inert control rather than an error (`/library` and `/wishlist` are one component over two routes). ⚠️ **`addedAt` describes an EVENT, not the item**, so a merge cannot resolve it by keeping the richer copy. ⚠️ A check naming one of two surfaces sharing a component is testing its luck. ⚠️ **A `route ===` condition inside such a component is a bug waiting for whichever route the nav prefers** — branch on `activeTab`; `route` is for building URLs. Pinned by `components/myStuffRouteConditions.test.ts`. → [[shared-view-two-routes]]
- **A control the shared toolbar renders on a tab must be consulted by that tab.** SubBar puts search, type chips, sort and the Filters sheet above every list page, and MyStuffView returned early for Progress with no state passed at all — four visible controls that did nothing for twelve days. Wire them or don't render them; "they don't apply here" is indistinguishable from broken. ⚠️ **Every filter in this app is client-side, so a filtered list must be fetched WHOLE**: a filter over one page of a paged list finds only what you already scrolled past. → [[shared-view-two-routes]]
- **A Settings save that changes a per-user preference MUST call `resetSessionProbe()`.** `sessionProbe` caches `/api/auth/me` in a module-level promise and every list surface reads a preference through it; client navigation does not remount the module, so without the reset the picker updates, the toast fires, the DB row changes, and **every list keeps the old value until a full reload** — indistinguishable from a broken setting. `savePlatforms` had it with a comment naming this exact failure while `saveMediaTypes` **twelve lines above it** did not, so reviewing one save function is not reviewing its neighbour. ⚠️ **A per-user setting is a DEFAULT, not a scope**: never hide the control that undoes it. Hiding a type's chip and guarding against a stale selection of it are two halves of one design — keep both or neither, or you ship an un-emptyable list. → [[user-display-preferences]]
- **A facet id is computed SERVER-side, always.** The list routes ship `sources[].data` as `{}`, and `extractFacets` reads people, companies and franchises straight out of those blobs, so a client-side re-derivation sees TAGS ONLY — and a pill matching nothing looks exactly like a genuine zero. Every route that filters carries `facetIds`; `matchesFacetIds` is the only matcher, and `matchesFacets` is deleted so `tsc` rejects the old shape. ⚠️ A PARTIAL `facetIds` (non-tag only, to save bytes) is `data: {}` wearing a different name: all four kinds or none. → `src/lib/listRouteFacetIds.test.ts`, [[shared-view-two-routes]]
- **An `/api/` response is compressed ONLY because `lib/compressResponse.ts` does it; Next will not.** A NEW route gets gzip only if it goes through `withUser`/`withOptionalUser` or wraps its own return; otherwise it ships raw. **Price a payload in GZIPPED bytes** (`/api/library` 9.77 → 2.23 MB), so a field-trimming idea is worth about a quarter of its raw figure. → `docs/archive/history.md` (grep `gzip /api/`), [[shared-view-two-routes]]
- **Anything a CLIENT component renders must never reach a module that imports `db.ts`.** The failure is a 500 on the page with tsc, lint and every test green, because nothing but loading the page exercises the client/server module graph. The fix is a LEAF module with no imports, shared by both halves. ⚠️ `import type` is erased and always safe; a runtime value is not. ⚠️ Turbopack caches the bad graph, so delete `.next` before believing a stale `Module not found`. → [[home-snapshot-and-crawlable-rails]]
- **CSS visibility is not conditional rendering.** A `lg:hidden` tree plus a `hidden lg:block` tree puts BOTH in the DOM, so every client island in them mounts twice and images inside `display:none` still download. Switch the *layout* with CSS and render the CONTENT once. Static paired title/nav elements are fine and deliberate (it removes one from the a11y tree). → [[item-detail-mockup-rebuild]]
- **A section above the fold that renders `null` while loading is a layout shift, and a skeleton alone cannot fix an auth-gated one**. The client only learns someone is signed in when the same round-trip lands. Read the session in `page.tsx` for a `signedIn` boolean and pass it down; a crawler is anonymous and gets byte-identical HTML. ⚠️ A placeholder must match the real box, MEASURED. The first attempt was 36px short, plus 4px of scroller padding, and those deltas are additive. → [[home-snapshot-and-crawlable-rails]]
- **One outcome per tap target, proven by hit-testing** (`document.elementFromPoint` across a grid of points inside a cell), not by reading the JSX. Two clickable layers in one cell means thumb placement decides what happens. **A hover affordance on a target only a thumb can reach is a defect on mobile, not a desktop bonus**. Decide which surface the control belongs to. → [[calendar-day-first-and-viewport-fit]]
- **A page-level `min-h-screen` inside a padded wrapper guarantees the page scrolls**: 100vh plus the padding exceeds the viewport by construction. `<body>` already carries it. Derive bottom padding from `--size-nav-bar-mobile` plus `env(safe-area-inset-bottom)`, never a flat guess. **Measure `scrollHeight - innerHeight` and expect 0.** → [[calendar-day-first-and-viewport-fit]]
- **When a surface must not jank, the fix is a layout that cannot change size, not smoother transitions on one that does.** Measure the budget with a `ResizeObserver` on a `flex-1` element (its height comes from the VIEWPORT, so it cannot feed itself), give children explicit pixel heights that sum back, and let the arithmetic decide the design. Give BOTH states of a disclosure the same size, and put a status line in a slot that always exists. ⚠️ `overflow-hidden` on the page root makes it the sticky SCROLLPORT. → [[calendar-day-first-and-viewport-fit]]
- **A `position: fixed` overlay is trapped by ANY ancestor with a `transform`, `translate`, `rotate`, `scale`, `filter` or `container-type` — including a ZERO-valued one.** Tailwind v4's `translate-y-0` sets the standalone `translate` property. ⚠️ Walking ancestors for `transform` will NOT find it (`transform` reads `none` while `translate` reads `0px`). The fix is a PORTAL to `document.body`, never a `max-height`. Diagnose against a control: append your own `fixed inset-0` div and compare heights. → [[fixed-overlay-containing-block]]
- **Three ways hover breaks, and one way a working hover LOOKS broken.** Tailwind v4's preflight leaves `<button>` at `cursor: default` (fix it once in `@layer base`). A `hover:bg-*` utility cannot beat an inline `style={{ background }}`: put the values in custom properties and let utilities switch them. ⚠️ **A browser pane that isn't displayed stops compositing AND starves the whole rendering loop** — `getComputedStyle` returns the START value of every transition, CLS reports zero either way, `(hover: hover)` is FALSE, and `requestAnimationFrame` / `ResizeObserver` / `IntersectionObserver` never fire. Inject `* { transition: none !important }` before measuring, and **run a control** against the old code. ⚠️ A tab never fronted reports `innerWidth: 0`, so every geometry read is garbage; assert `innerWidth > 0` in the same probe. → [[hover-and-cursor-traps]]
- **The Google "G" is the ONE exception to the no-brand-colour rule, and it is required, not chosen.** Google's guidelines list monochrome versions under **Don't** and make compliance a condition of app verification. Drawn by `components/auth/GoogleMark.tsx` with four literal fills, never by `BrandGlyph` or `brandMarks.ts`; `brandMarksNoGoogle.test.ts` fails if a `Google` key reappears, because a monochrome G looks fine and is a silent breach. ⚠️ Read the guidelines in **ENGLISH**: the German translation inverts the background-colour rule. → [[identity-only-auth-providers]]
- **Platform colour-coding is removed site-wide. Identify a provider by its LOGO via `<BrandGlyph source="…">`, not its hex.** No brand hue in ANY state, hover included. `TYPE_COLORS` (game/movie/show) is a different axis and stays. ⚠️ The sweep missed one inline hex for five days, so **grep for a hex** before repeating "site-wide". simple-icons ships WORDMARKS that are illegible below ~20px. → [[platform-brand-marks-not-colours]]
- **"The nav reaches every page" is not the same claim for every session state.** `AppNav`'s "You" slot is a link for a signed-in visitor and a `SignInDialog` button for an anonymous one, and `/profile` additionally `router.replace("/")`s anon before its footer renders. Verify a reachability claim separately for every auth branch the nav renders differently for. → [[anon-legal-reachability]]
- **`truncate` does nothing without `min-w-0`.** Verify any mobile layout at 320/360/412. → [[mobile-viewport-overflow]]
- **`/library`, `/wishlist`, `/settings` and the FACET pages are dead under `next dev` only**. Use the `prod` launch config to verify them. ⚠️ `/settings` renders the SIGNED-IN chrome with every field empty rather than a spinner, which reads as data loss. → [[unhydrated-page-diagnosis]]

### Design tokens

- **A `@theme` custom property read ONLY through an inline `var()` is TREE-SHAKEN**. It resolves to `""`, the declaration is invalid, and the element inherits. No build, lint or test error. Declare such tokens in a plain `:root` block instead. Check with `getComputedStyle(document.documentElement).getPropertyValue('--your-token')` returning `""`. → [[tailwind-theme-tree-shaking]]
- **A token's `@theme` prefix must be the utility's OWN theme key, and a wrong prefix generates NO RULE AT ALL.** `duration-*` resolves from `--transition-duration-*`, so `--duration-base` left ~28 call sites at Tailwind's default for three weeks with everything green. **Read the utility's real theme key** (`grep -o '"duration",[^;]\{0,300\}' node_modules/tailwindcss/dist/lib.js`) and **verify a matching RULE exists, not that the token resolves**. ⚠️ A naive `document.styleSheets` walk gives a FALSE NEGATIVE (CSS nesting empties `.cssRules`); test `selectorText` first. ⚠️ The scanner reads MARKDOWN, so doc prose emits real rules: "the rule exists in prod" is not proof a component uses it. → [[tailwind-theme-tree-shaking]]
- **`personKey`/`slugify`: NFD does not decompose `ø å æ ł ß`.** Never give `tagKey` the same fix. → [[unicode-normalization-lossy-slugs]]
- **Rank a selection by the same quantity the thing it feeds is made of, and name the input a row was actually computed from.** The Score's top-N buckets sorted by raw `dev` while a facet contributes `dev · classWeight · gain`, so the panel printed a greyed "not counted" row worth twice the counted one above it. ⚠️ `CATEGORY_LABELS` is nine STATIC ids and the taxonomy is EDITABLE: resolve a label against the live list, fall back to the id, never to a flat "Tag". ⚠️ `docs/fandex-score.md` §3.3 specified the correct sort from day one and the code never matched it; **diff a spec that specific against the implementation**. → [[fandex-score-h5]]

### Tooling and lint

- **A type-only import must be written `import type`**, an eslint ERROR here, and not style. Node's native type-stripping (what `scripts/alias-hooks.mjs` gives every standalone `.mjs`) erases only *syntactically* type-only constructs, so a plain `import { Foo }` of an interface throws `SyntaxError` the moment a script reaches that module, while tsc, vitest, `next dev` and `next build` all elide it silently. Every export of `src/types/index.ts` is type-only. → [[db-migrations-and-testing]]
- **Check for a trailing newline before appending to a config file, and never let a secret reach the terminal.** `printf '…' >> .env` on a file with no final newline lands on the END of the last line: it concatenated onto `DISCORD_CLIENT_SECRET`'s value, and repairing it meant reading the line back, so the secret hit the transcript and had to be rotated. The `node -e` rule below covers TRACKED files; `.env` is untracked, which is exactly why it was not covered. ⚠️ **A secret that reaches a transcript is burned** — say so plainly and put the rotation at the top of Nils's list rather than quietly repairing the file. → [[identity-only-auth-providers]]
- **Never edit a repo file with `node -e` string slicing. Use the Edit tool.** This checkout is **CRLF**, so `indexOf('\n\n')` never matches, returns -1, and `s.slice(-1)` keeps ONE CHARACTER instead of the rest of the file. It deleted 236 of `TASKS.md`'s 254 lines and was committed unnoticed; nothing threw and the script printed "ok". ⚠️ Its sibling: **backticks inside a `node -e` string are command substitution in bash**, so a `` `path.tsx` `` in doc prose is executed and replaced with nothing. If you must script an edit, verify the line count before AND after.
- **Read eslint's `✖` TOTALS line, never `tail` its output.** The LAST line is the FIXABLE summary — "0 errors and 8 warnings potentially fixable" means none of the errors are auto-fixable, NOT that there are none. The totals line sits above it: "481 problems (1 error, 480 warnings)". A `| tail -2` reads as green and let a CI-breaking error through. `npm run lint 2>&1 | grep -E '^✖'`.
- **Do not run `eslint --fix` broadly without diffing for non-import changes**. It rewrote three `{/* eslint-disable-next-line */}` JSX comments to a bare `{ }`, destroying the directives while looking inert. → [[eslint-fix-mangles-jsx-comments]]
- **A `//` comment in JSX CHILDREN position is a TEXT NODE and renders on the page.** A *refactoring* hazard: wrapping an existing `return ( … )` in a fragment moves any `//` comment from JS position into children position. Valid JSX, so tsc, lint, tests and the build all pass. Only a human looking at the page catches it. `react/jsx-no-comment-textnodes` is now an ERROR. Re-check every comment inside a `return` you just wrapped. → [[jsx-comment-in-children-renders]]
- **`npm run lint` scans the whole cwd with no path filter**, so a stray `git worktree` under `.claude/worktrees/` gets scanned, gitignored or not (`eslint.config.mjs`'s `globalIgnores` excludes it now). If lint reports a wall of errors you don't recognize, check `git worktree list` before assuming you broke something, and **never delete a worktree you didn't create**. It may be another session's live work. Browser-verifying from a worktree needs a real `npm ci`; a `node_modules` junction fails. → [[worktree-needs-real-npm-ci]]
- **The `react-hooks/*` rules are ERRORS and the repo is error-clean.** Remaining `set-state-in-effect` flags sit on genuinely idiomatic patterns (storage hydration, prop→state sync, fetch-on-mount, load guards, layout measurement) with justified disable comments. Don't remove one without checking why it's there.
- **A push that doesn't reach prod is a CI problem until proven otherwise**. Healthy push→live is 3–5 min. → [[ci-red-blocks-railway-deploy]]

### Measurement discipline

- **Measure before optimising.** §A was mis-sized by 100× once, and a 58 s cold Discover load was blamed on the catalog pool for days when the pool rebuild is ~430 ms. Payload beat CPU in the one full audit. → [[perf-audit-2026-07-30]]
- **Take a time series, never a spot sample**, for prod memory, cost or DB growth. Across four incidents the obvious culprit was wrong every time, and **a WAL that will not truncate is a symptom to chase, not a high-water mark to document** (`shadowWalMb` moving across a restart separates the two). → [[prod-incidents]], [[wal-highwater-vs-litestream-stall]]
- **Price a feature in RAM, not disk**. Memory is 77% of the Railway bill and volume ~9%, so disk is ~75× cheaper. Catalog size is a resident-pool cost first. → [[railway-cost-shape]]
## Model/agent routing for this repo

The main session's model is whatever the human picked — this doesn't change that. It guides which model/agent tier to reach for when *delegating* via the Agent/Task/Workflow tools:

- **Bulk file search / "where is X" exploration** → the `Explore` agent at the default model. Cheap, fast, keeps the main context clean.
- **Mechanical doc maintenance** (archiving a finished phase, reformatting, renaming) → fine to delegate at low effort. Low blast radius, easy to review the diff afterward.
- **Anything touching `migrations.ts`, `matcher.ts`'s write paths, sync/pull adapters, or auth/session code** → do NOT delegate to a background/low-effort agent. These carry the invariants above (a mistake risks data loss or an auth hole) — do this work in the main loop at full effort, and verify against real `data/rr.db` behavior rather than trusting green tests alone (see [[db-migrations-and-testing]]: every DB test starts fresh, so none of them exercise the upgrade path production actually takes).
